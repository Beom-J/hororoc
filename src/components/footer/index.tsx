// @flow
import * as React from "react";
import * as S from "./style";

type Props = {};
export const Footer = (props: Props) => {
  return (
    <S.Footer>
      <span>menu bar!</span>
    </S.Footer>
  );
};
