// @flow
import * as React from "react";
import { HeaderBar } from "./style";
type Props = {};
export const Header = (props: Props) => {
  return (
    <HeaderBar>
      <span>header!</span>
    </HeaderBar>
  );
};
