// @flow
import * as React from "react";
import { Footer } from "./style";

type Props = {};
export const MenuBar = (props: Props) => {
  return (
    <Footer>
      <span>menu bar!</span>
    </Footer>
  );
};
